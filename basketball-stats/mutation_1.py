def bball_stats(twos: int, threes: int, shotsTaken: int):
  '''
    Wrong Sign
  '''
    import math 
    points = (twos * 2) + (threes * 3)
    FG = str(math.floor(((twos + threes) // shotsTaken)  + "%"
    return [points, FG]
bball_stats
